# Sirius.Achievements

**Веб-платформа учёта и управления достижениями студентов**

> Курсовой проект — Информационные системы и технологии
> Образовательный центр «Сириус», 2026

---

## Описание

**Sirius.Achievements** — полнофункциональная веб-платформа для учёта, модерации и анализа достижений студентов образовательного центра. Система автоматизирует процесс подачи, проверки и утверждения достижений, формирует рейтинги студентов и генерирует AI-сводки профилей на основе загруженных документов.

### Цель работы

Разработка информационной системы, обеспечивающей цифровизацию процесса учёта студенческих достижений с поддержкой модерации, геймификации и интеллектуального анализа документов.

### Задачи

1. Проектирование архитектуры и базы данных системы
2. Реализация системы аутентификации и авторизации с разграничением ролей
3. Разработка модуля загрузки и модерации достижений
4. Создание рейтинговой системы с сезонной механикой
5. Интеграция OCR-распознавания текста из документов (грамоты, сертификаты)
6. Реализация AI-генерации сводки профиля (локальная LLM Qwen2.5-7B-Instruct-AWQ через vLLM)
7. Разработка системы поддержки с чатом в реальном времени
8. Внедрение системы уведомлений через WebSocket
9. Развёртывание в контейнерной среде Docker

---

## Технологический стек

### Бэкенд

| Компонент | Технология | Версия |
|-----------|-----------|--------|
| Язык | Python | 3.11 |
| Веб-фреймворк | FastAPI | 0.115.14 |
| ASGI-сервер | Uvicorn | 0.34.3 |
| ORM | SQLAlchemy (async) | 2.0.41 |
| Миграции | Alembic | 1.16.2 |
| Валидация | Pydantic | 2.11.7 |
| Аутентификация | python-jose (JWT), bcrypt, passlib | — |
| Email | aiosmtplib | 3.0.2 |
| HTTP-клиент | httpx | 0.28.1 |
| Логирование | structlog | 24.1.0 |
| Мониторинг | Sentry SDK | 2.32.0 |

### Базы данных и кэширование

| Компонент | Технология | Версия |
|-----------|-----------|--------|
| СУБД | PostgreSQL | 15 |
| Драйвер | asyncpg | >= 0.30.0 |
| Кэш / Rate Limiting | Redis | 7 |

### OCR и AI

| Компонент | Технология | Назначение |
|-----------|-----------|-----------|
| OCR (изображения) | EasyOCR + PyTorch | Распознавание текста из грамот/сертификатов |
| OCR (PDF) | PyMuPDF | Извлечение текста из PDF-документов |
| AI-генерация | Qwen2.5-7B-Instruct-AWQ (vLLM, локально) | Формирование сводки профиля |

### Фронтенд

| Компонент | Технология | Назначение |
|-----------|-----------|-----------|
| Шаблонизатор | Jinja2 | Серверный рендеринг страниц |
| Реактивность | Alpine.js | Интерактивность на клиенте |
| Стилизация | Tailwind CSS | Адаптивная вёрстка |
| Графики | Chart.js | Визуализация статистики |
| Уведомления | WebSocket | Доставка в реальном времени |

### Инфраструктура

| Компонент | Технология |
|-----------|-----------|
| Контейнеризация | Docker, Docker Compose |
| Тестирование | pytest, pytest-asyncio |
| Базовый образ | python:3.11-slim |

---

## Функциональные возможности

### Роли пользователей

| Роль | Описание | Возможности |
|------|---------|------------|
| **Гость** (GUEST) | Незарегистрированный / ожидающий подтверждения | Регистрация, подтверждение email |
| **Студент** (STUDENT) | Активный участник | Загрузка достижений, просмотр рейтинга, обращения в поддержку, профиль |
| **Модератор** (MODERATOR) | Проверяющий | Модерация достижений и пользователей, чат поддержки, назначение работы |
| **Супер-администратор** (SUPER_ADMIN) | Полный доступ | Управление всей системой, завершение сезонов, CMS-страницы |

### Модуль достижений

- Загрузка документов (PDF, JPEG, PNG, WebP) с валидацией формата и размера
- Категоризация: Спорт, Наука, Искусство, Волонтёрство, Другое
- Уровни: Школьный, Муниципальный, Региональный, Федеральный, Международный
- Автоматический расчёт баллов по уровню достижения
- Статусы: Ожидание → Одобрено / Отклонено / На доработке
- Предпросмотр документов в модальном окне

### Система модерации

- Трёхступенчатый интерфейс: **Входящие** → **Моя работа** → **Все записи**
- Назначение модератора на конкретное достижение/пользователя (кнопка «Взять в работу»)
- Возможность отказаться от взятой работы
- Зональный доступ: модератор видит только свой уровень образования
- Одобрение / отклонение с указанием причины
- Статусы в таблицах: Новый, В работе, Занят

### Рейтинговая система

- Глобальный и лиговый рейтинг студентов
- Фильтрация по уровню образования, курсу и направлению
- Топ-3 с карточками, остальные — в таблице
- Кнопка «Найти меня» — автопрокрутка к позиции пользователя с подсветкой
- Сезонная механика: завершение сезона с фиксацией результатов и обнулением баллов
- Экспорт рейтинга в CSV

### Система поддержки

- Создание тикетов с темой и сообщением
- Чат между студентом и модератором с вложениями файлов
- Назначение модераторов на тикеты
- Автоархивация неактивных тикетов (90 дней)
- Настраиваемые сроки сессий (день, неделя, месяц)

### AI-сводка профиля

- OCR-распознавание текста из загруженных грамот и сертификатов (EasyOCR)
- Извлечение текста из PDF-документов (PyMuPDF)
- Генерация структурированной сводки через локальную LLM Qwen2.5-7B-Instruct-AWQ (vLLM)
- Генерация по шаблону при недоступности LLM (статистика + список достижений)
- Кэширование результата, регенерация при появлении новых документов

### Уведомления

- Доставка в реальном времени через WebSocket
- Хранение в базе данных с отметками о прочтении
- Ссылки для перехода к событию

### Тёмная тема

- Полная поддержка светлой и тёмной палитры
- Анимированный переключатель (солнце/луна)
- Конфигурируемые цвета через `theme.conf`
- Сохранение выбора в localStorage

---

## Архитектура проекта

### Структура каталогов

```
sirius_achievements/
├── app/                          # Ядро приложения
│   ├── models/                   # ORM-модели (11 сущностей)
│   │   ├── user.py               # Пользователи
│   │   ├── achievement.py        # Достижения
│   │   ├── support_ticket.py     # Тикеты поддержки
│   │   ├── support_message.py    # Сообщения поддержки
│   │   ├── notification.py       # Уведомления
│   │   ├── user_token.py         # JWT и токены сброса
│   │   ├── page.py               # CMS-страницы
│   │   ├── audit_log.py          # Журнал аудита
│   │   ├── season_result.py      # Сезонные результаты
│   │   └── enums.py              # Перечисления (роли, статусы)
│   ├── routers/                  # Маршруты (контроллеры)
│   │   ├── admin/                # Веб-интерфейс
│   │   │   ├── auth.py           # Аутентификация
│   │   │   ├── dashboard.py      # Дашборд
│   │   │   ├── moderation.py     # Модерация
│   │   │   ├── my_work.py        # Моя работа (назначения)
│   │   │   ├── pages.py          # CMS-страницы
│   │   │   └── ...
│   │   └── api/                  # REST API (JWT)
│   ├── services/                 # Бизнес-логика
│   │   ├── auth_service.py       # Аутентификация
│   │   └── admin/
│   │       ├── resume_service.py # OCR + AI-генерация сводки
│   │       └── ...
│   ├── repositories/             # Слой доступа к данным (CRUD)
│   ├── schemas/                  # Pydantic-схемы валидации
│   ├── infrastructure/           # БД, JWT, логирование
│   ├── middlewares/              # CSRF, безопасность, лимиты загрузок
│   ├── security/                 # CSRF-токены, JWT-утилиты
│   ├── utils/                    # Rate limiter, уведомления, валидация
│   ├── seeders/                  # Начальное наполнение БД
│   └── tests/                    # Тесты
├── templates/admin/              # Jinja2 HTML-шаблоны
│   ├── auth/                     # Страницы входа, регистрации
│   ├── dashboard/                # Дашборд
│   ├── moderation/               # Входящие (модерация)
│   ├── my_work/                  # Моя работа
│   ├── leaderboard/              # Рейтинг
│   ├── profile/                  # Профиль
│   ├── users/                    # Управление пользователями
│   ├── documents/                # Управление документами
│   ├── support/                  # Поддержка
│   ├── partials/                 # Компоненты (тема, кнопки)
│   └── layout.html               # Базовый шаблон
├── static/                       # Статические файлы
│   └── theme/                    # Тёмная тема (CSS, JS, иконки, конфиг)
├── models/                       # Предобученные модели OCR
│   ├── craft_mlt_25k.pth        # Модель детекции текста
│   └── cyrillic_g2.pth          # Модель распознавания кириллицы
├── main.py                       # Точка входа FastAPI
├── docker-compose.yml            # Оркестрация контейнеров
├── Dockerfile                    # Сборка Docker-образа
├── entrypoint.sh                 # Скрипт запуска
├── requirements.txt              # Python-зависимости
└── .env.example                  # Шаблон переменных окружения
```

### Схема взаимодействия компонентов

```
┌──────────┐     HTTP/WS      ┌──────────────┐     async      ┌────────────┐
│  Клиент  │ ◄──────────────► │   FastAPI     │ ◄────────────► │ PostgreSQL │
│ (Браузер)│                  │   (Uvicorn)   │                └────────────┘
└──────────┘                  │               │     async      ┌────────────┐
                              │  Middlewares  │ ◄────────────► │   Redis    │
                              │  ├── CSRF     │                └────────────┘
                              │  ├── Security │
                              │  └── Upload   │     HTTP       ┌────────────┐
                              │               │ ──────────────►│    vLLM    │
                              │  Services     │                │(Qwen2.5-7B) │
                              │               │                └────────────┘
                              │  ├── Auth     │
                              │  ├── Resume   │  EasyOCR       ┌────────────┐
                              │  └── Support  │ ──────────────►│ OCR Models │
                              └──────────────┘                 └────────────┘
```

### Модели базы данных

| Модель | Таблица | Описание | Ключевые поля |
|--------|---------|---------|---------------|
| `Users` | `users` | Пользователи | role, status, education_level, course, resume_text |
| `Achievement` | `achievements` | Достижения | category, level, points, status, moderator_id |
| `SupportTicket` | `support_tickets` | Тикеты поддержки | status, moderator_id, session_expires_at |
| `SupportMessage` | `support_messages` | Сообщения в тикетах | text, file_path, is_from_moderator |
| `Notification` | `notifications` | Уведомления | title, message, is_read, link |
| `UserToken` | `user_tokens` | Токены (JWT, сброс пароля) | token, token_type, expires_at |
| `Page` | `pages` | CMS-страницы | title, slug, content |
| `AuditLog` | `audit_logs` | Журнал аудита | action, target_type, ip_address |
| `SeasonResult` | `season_results` | Результаты сезонов | season_name, points, rank |

---

## Установка и запуск

### Системные требования

- **Docker** и **Docker Compose** (рекомендуемый способ)
- **4 ГБ RAM** минимум (PyTorch + EasyOCR)
- Модели OCR: `craft_mlt_25k.pth`, `cyrillic_g2.pth` в папке `models/`
- Для AI-сводки (опционально): **NVIDIA GPU** (~8+ ГБ VRAM) с установленным `nvidia-container-toolkit` — для сервиса `vllm`, обслуживающего Qwen2.5-7B-Instruct-AWQ. Без GPU функцию можно оставить выключенной (`RESUME_LOCAL_AI_ENABLED=False`) — сводка тогда будет собираться по шаблону, без LLM.

### 1. Клонирование репозитория

```bash
git clone https://github.com/RochDM231718/sirius_achievements.git
cd sirius_achievements
```

### 2. Настройка переменных окружения

```bash
cp .env.example .env
```

Откройте `.env` и заполните обязательные параметры:

| Переменная | Описание | Пример |
|-----------|---------|--------|
| `DB_USERNAME` | Пользователь PostgreSQL | `sirius_user` |
| `DB_PASSWORD` | Пароль PostgreSQL | `secure_password` |
| `DB_NAME` | Имя базы данных | `sirius_db` |
| `REDIS_PASSWORD` | Пароль Redis | `redis_password` |
| `SECRET_KEY` | Секретный ключ приложения | Сгенерировать: `openssl rand -base64 32` |
| `ALLOWED_HOSTS` | Разрешённые хосты | `localhost,127.0.0.1` |
| `MAIL_HOST` | SMTP-сервер | `smtp.yandex.ru` |
| `MAIL_USERNAME` | Email для отправки | `support@example.ru` |
| `MAIL_PASSWORD` | Пароль приложения | — |

Опциональные (AI-сводка, требует GPU и сервис `vllm` из docker-compose.yml):

| Переменная | Описание |
|-----------|---------|
| `LOCAL_LLM_BASE_URL` | Адрес OpenAI-совместимого API vLLM (по умолчанию `http://vllm:8000/v1`) |
| `LOCAL_LLM_MODEL` | Название модели (по умолчанию `Qwen/Qwen2.5-7B-Instruct-AWQ`) |
| `RESUME_LOCAL_AI_ENABLED` | Включить локальную LLM для генерации сводки (`True`/`False`) |

### 3. Запуск через Docker

```bash
docker compose up -d --build
```

Будут запущены 3 контейнера: `sirius_db_new` (PostgreSQL), `sirius_redis_new` (Redis), `sirius_app_new` (FastAPI, порт **8081**).

Сервис `vllm` (Qwen2.5-7B-Instruct-AWQ, для AI-сводки) не запускается по умолчанию, так как требует NVIDIA GPU. Чтобы включить его:

```bash
# 1. Установите nvidia-container-toolkit на хосте
# 2. В .env установите RESUME_LOCAL_AI_ENABLED=True
# 3. Запустите с профилем ai
docker compose --profile ai up -d --build
```

Появится 4-й контейнер `sirius_vllm_new` (порт 8000, внутренний). Без GPU функцию можно оставить выключенной — сводка будет собираться по шаблону, без LLM.

### 4. Проверка

```bash
# Логи приложения
docker logs sirius_app_new --tail 20

# Ожидаемый вывод:
# Application startup complete.
```

Приложение доступно: **http://localhost:8081/sirius.achievements/**

### 5. Начальные данные (опционально)

```bash
# Применение миграций
docker exec sirius_app_new alembic upgrade head

# Заполнение тестовыми данными
docker exec sirius_app_new python app/seeders/main.py
```

---

## Локальный запуск (без Docker)

```bash
# 1. Виртуальное окружение
python -m venv .venv
source .venv/bin/activate        # Linux/macOS
.venv\Scripts\activate           # Windows

# 2. Зависимости
pip install -r requirements.txt

# 3. Переменные окружения
cp .env.example .env
# Настроить подключение к PostgreSQL и Redis

# 4. Миграции
alembic upgrade head

# 5. Запуск
uvicorn main:app --reload --port 8000
```

---

## Конфигурация

### Система начисления баллов

| Уровень достижения | Баллы | Переменная окружения |
|--------------------|---------|--------------------|
| Школьный | 10 | `POINTS_SCHOOL` |
| Муниципальный | 20 | `POINTS_MUNICIPAL` |
| Региональный | 40 | `POINTS_REGIONAL` |
| Федеральный | 75 | `POINTS_FEDERAL` |
| Международный | 100 | `POINTS_INTERNATIONAL` |

### Rate Limiting (защита от перебора)

| Параметр | Значение | Описание |
|---------|---------|---------|
| `LOGIN_MAX_ATTEMPTS` | 5 | Попыток входа до блокировки |
| `LOGIN_LOCKOUT_TTL` | 900 | Блокировка на 15 минут (сек) |
| `UPLOAD_MAX_PER_HOUR` | 20 | Лимит загрузок файлов в час |
| `FORGOT_PWD_MAX_ATTEMPTS` | 5 | Попыток сброса пароля |

### Лимиты загрузки файлов

| Тип файла | Лимит | Переменная |
|----------|-------|-----------|
| Аватар | 2 МБ | `MAX_AVATAR_SIZE` |
| Документ | 10 МБ | `MAX_DOC_SIZE` |
| Файл поддержки | 5 МБ | `MAX_SUPPORT_FILE_SIZE` |

---

## API

### REST API (JWT-аутентификация)

```http
POST   /api/login                — Получение access и refresh токенов
POST   /api/refresh              — Обновление access-токена
GET    /api/my-achievements      — Список достижений текущего пользователя
POST   /api/achievements         — Загрузка нового достижения
```

### WebSocket

```
WS     /ws/notifications         — Уведомления в реальном времени
```

### Health Check

```http
GET    /health                   — Статус приложения (200 OK)
```

---

## Тестирование

```bash
# В контейнере
docker exec sirius_app_new pytest app/tests/ -v

# Локально
pytest app/tests/ -v
```

---

## Полезные команды

```bash
# Пересборка и запуск
docker compose up -d --build

# Просмотр логов
docker logs sirius_app_new -f

# Создание миграции
docker exec sirius_app_new alembic revision --autogenerate -m "описание"

# Применение миграций
docker exec sirius_app_new alembic upgrade head

# Откат миграции
docker exec sirius_app_new alembic downgrade -1

# Подключение к БД
docker exec -it sirius_db_new psql -U <DB_USERNAME> -d <DB_NAME>

# Подключение к Redis
docker exec -it sirius_redis_new redis-cli -a <REDIS_PASSWORD>
```

---

